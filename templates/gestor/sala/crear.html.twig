{% extends 'gestor/layout_dashboard.html.twig' %}
{% set titulo = 'Crear nueva sala' %}

{% block contenido %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
        <h1 class="h2">{{ titulo }}</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group mr-2">
                <a href="{{ path('gestor_sala_listar') }}" class="btn btn-sm btn-danger"><span
                            data-feather="arrow-left-circle"></span> Cancelar y volver</a>
            </div>
            <button class="btn btn-sm btn-outline-secondary dropdown-toggle">
                <span data-feather="calendar"></span>
                This week
            </button>
        </div>
    </div>

    <div class="col-md-8 order-md-1">
        <form class="form" action="{{ path('gestor_sala_grabar') }}" method="post">
            <div class="form-group">
                <label for="nombre">Nombre</label>
                <input class="form-control" name="nombre" type="text">
            </div>

            <div class="form-group">
                <label for="direccion">Dirección</label>
                <input class="form-control" name="direccion" type="text">
            </div>

            <div class="form-group">
                <label for="provincia_id">Provincia</label>
                <select class="form-control" name="provincia_id">
                    {% for provincia in provincias %}
                        <option value="{{ provincia.id }}">{{ provincia.nombre }}</option>
                    {% endfor %}
                </select>
            </div>

            <div class="form-group">
                <label for="localidad">Localidad</label>
                <input class="form-control" name="localidad" type="text">
            </div>

            <div class="form-group">
                <label for="mapa_url">URL Mapa</label>
                <input class="form-control" name="mapa_url" type="url">
            </div>

            <div class="form-group">
                <label for="aforo">Aforo</label>
                <input class="form-control" name="aforo" type="number" max="1000" min="0">
            </div>

            <button class="btn btn-primary btn-lg btn-block" type="submit">Grabar Sala</button>

        </form>
    </div>
{% endblock %}


